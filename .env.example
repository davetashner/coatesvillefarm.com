# =============================================================================
# Coatesville Farm Frontend Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values
# These are embedded at build time (not runtime)

# -----------------------------------------------------------------------------
# Contact Form API
# -----------------------------------------------------------------------------
# Get this URL from the CDK deployment output:
#   cd infrastructure && npx cdk deploy
# Look for the "ApiEndpoint" output value
VITE_CONTACT_API_URL=https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com/prod/contact

# -----------------------------------------------------------------------------
# Google reCAPTCHA v3
# -----------------------------------------------------------------------------
# Get your site key from: https://www.google.com/recaptcha/admin
# 1. Sign in with a Google account
# 2. Create a new site with reCAPTCHA v3 (score-based)
# 3. Add your domain (coatesvillefarm.com) and localhost for testing
# 4. Copy the SITE KEY here (NOT the secret key - that goes in Lambda)
#
# If not set, the form still works using honeypot + rate limiting
VITE_RECAPTCHA_SITE_KEY=6Lxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
